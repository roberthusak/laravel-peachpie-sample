--- tests/Integration/Database/MigrateWithRealpathTest.php
+++ tests/Integration/Database/MigrateWithRealpathTest.php
@@ -15,10 +15,10 @@ class MigrateWithRealpathTest extends DatabaseTestCase
             '--realpath' => true,
         ];
 
-        $this->artisan('migrate', $options);
+        $this->artisan('migrate', $options)->run();
 
         $this->beforeApplicationDestroyed(function () use ($options) {
-            $this->artisan('migrate:rollback', $options);
+            $this->artisan('migrate:rollback', $options)->run();
         });
     }
 

--- tests/Integration/Database/RefreshCommandTest.php
+++ tests/Integration/Database/RefreshCommandTest.php
@@ -30,14 +30,14 @@ class RefreshCommandTest extends DatabaseTestCase
     private function migrate_refresh_with(array $options)
     {
         $this->beforeApplicationDestroyed(function () use ($options) {
-            $this->artisan('migrate:rollback', $options);
+            $this->artisan('migrate:rollback', $options)->run();
         });
 
-        $this->artisan('migrate:refresh', $options);
+        $this->artisan('migrate:refresh', $options)->run();
         DB::table('members')->insert(['name' => 'foo', 'email' => 'foo@bar', 'password' => 'secret']);
         $this->assertEquals(1, DB::table('members')->count());
 
-        $this->artisan('migrate:refresh', $options);
+        $this->artisan('migrate:refresh', $options)->run();
         $this->assertEquals(0, DB::table('members')->count());
     }
 }

--- tests/Integration/Database/MigratorEventsTest.php
+++ tests/Integration/Database/MigratorEventsTest.php
@@ -24,8 +24,8 @@ class MigratorEventsTest extends DatabaseTestCase
     {
         Event::fake();
 
-        $this->artisan('migrate', $this->migrateOptions());
-        $this->artisan('migrate:rollback', $this->migrateOptions());
+        $this->artisan('migrate', $this->migrateOptions())->run();
+        $this->artisan('migrate:rollback', $this->migrateOptions())->run();
 
         Event::assertDispatched(MigrationsStarted::class, 2);
         Event::assertDispatched(MigrationsEnded::class, 2);
@@ -37,8 +37,8 @@ class MigratorEventsTest extends DatabaseTestCase
     {
         Event::fake();
 
-        $this->artisan('migrate', $this->migrateOptions());
-        $this->artisan('migrate:rollback', $this->migrateOptions());
+        $this->artisan('migrate', $this->migrateOptions())->run();
+        $this->artisan('migrate:rollback', $this->migrateOptions())->run();
 
         Event::assertDispatched(MigrationStarted::class, function ($event) {
             return $event->method == 'up' && $event->migration instanceof Migration;
@@ -58,8 +58,8 @@ class MigratorEventsTest extends DatabaseTestCase
     {
         Event::fake();
 
-        $this->artisan('migrate');
-        $this->artisan('migrate:rollback');
+        $this->artisan('migrate')->run();
+        $this->artisan('migrate:rollback')->run();
 
         Event::assertDispatched(NoPendingMigrations::class, function ($event) {
             return $event->method == 'up';

--- tests/Integration/Console/ConsoleApplicationTest.php
+++ tests/Integration/Console/ConsoleApplicationTest.php
@@ -61,7 +61,7 @@ class ConsoleApplicationTest extends TestCase
 
         $this->assertFalse($this->app->resolved(Schedule::class));
 
-        $this->artisan('foo:schedule');
+        $this->artisan('foo:schedule')->run();
 
         $this->assertTrue($this->app->resolved(Schedule::class));
     }
